# Required: server-side OpenAI key. Never expose with NEXT_PUBLIC_*.
OPENAI_API_KEY=sk-replace-me

# Optional: override the model. Defaults to gpt-4o-mini for cost-efficient MVP.
OPENAI_MODEL=gpt-4o-mini
